// Root-level sanity.config.ts — re-exports from sanity/ workspace
// This file exists so the Sanity CLI can detect the project root.
// The actual Studio config is in ./sanity/sanity.config.ts
export { default } from './sanity/sanity.config'
